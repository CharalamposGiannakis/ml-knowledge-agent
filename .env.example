# Copy to `.env` and fill in real values. `.env` is gitignored.

# Required for Fuseki admin operations (init_fuseki.sh, load_data.sh)
FUSEKI_ADMIN_PASSWORD=change-me

# Required for Phase 2 extraction pipeline (scripts/phase2_extract.py)
ANTHROPIC_API_KEY=sk-ant-your-key-here
